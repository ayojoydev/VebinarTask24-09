def area(side):
    if side <= 0:
        return "Invalid side"
    else:
        return side**2

def rectangleference(side):
    if side <= 0:
        return "Invalid side"
    else:
        return side*4
